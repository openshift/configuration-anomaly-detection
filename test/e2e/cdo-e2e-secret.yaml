apiVersion: template.openshift.io/v1
kind: Template
metadata:
  name: template
objects:
  - apiVersion: v1
    kind: Secret
    metadata:
      name: configuration-anomaly-detection-sc
    stringData:
      CAD_PAGERDUTY_ROUTING_KEY: ${CAD_PAGERDUTY_ROUTING_KEY}
parameters:
  - name: CAD_PAGERDUTY_ROUTING_KEY
    required: true
